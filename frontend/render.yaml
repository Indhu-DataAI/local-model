services:
  - type: web
    name: local-model
    env: node
    buildCommand: cd frontend && npm install && npm run build
    startCommand: serve -s dist
    staticBuildPath: frontend/dist
    envVars:
      - key: NODE_ENV
        value: production
